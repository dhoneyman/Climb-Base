<div class="row">

  <div class="text-center">

    <h2>{{name}} - </h2><h5>{{difficulty}}</h5>
    <p>First Ascent by {{FA}}</p>
<div class='col s6'>
<form action="#">
    <p class="range-field">
      <input type="range" id="rating" min="0" max="5" />
    </p>
  </form>
  </div>
    <textarea id='new-comment' placeholder="Leave a Review!"></textarea>

    <div class="form-group">
      <button class="btn btn-primary" type="button" id='comment-btn'>Submit</button>
    </div>

  </div>

    <div>
      {{#each ratings as |rating|}}

{{!-- <div class="row">
    <div class="col s12 m6">
      <div class="card blue-grey darken-1">
        <div class="card-content white-text">
          Reviewer
          {{user.rating.user_id}}
        <div class="card-action">
        </div>
      <p>{{rating.rating}} Stars - {{rating.review}}</p>
          
        </div>
      </div>
    </div>
  </div> --}}


<br>


<div>
  {{!-- {{user.}} --}}
  Geoff
<div>
  <blockquote>{{get_emoji rating.rating}} - {{rating.review}} </blockquote>
</div>
</div>


      {{/each}}
    </div>
</div>

<script src="/js/comment.js"></script>
<script src="/utils/helpers.js"></script>

